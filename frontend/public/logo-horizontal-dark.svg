<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 600 180" width="100%" height="100%" fill="none">
  <!-- Fondo Dark Charcoal -->
  <rect width="600" height="180" rx="24" fill="#090d16" />

  <!-- Isotipo P -->
  <g transform="translate(18, 0)">
    <!-- Tallo: Ámbar Solar -->
    <rect x="24" y="24" width="28" height="132" rx="14" fill="#f59e0b" />
    <!-- Barra Superior: Películas -->
    <rect x="64" y="24" width="75" height="28" rx="14" fill="#4ade80" />
    <!-- Barra Media: Manga / Lectura -->
    <rect x="64" y="60" width="98" height="28" rx="14" fill="#60a5fa" />
    <!-- Barra Inferior: Videojuegos -->
    <rect x="64" y="96" width="63" height="28" rx="14" fill="#c084fc" />
  </g>

  <!-- Tipografía Pathd -->
  <text x="204" y="122" font-family="system-ui, -apple-system, 'Inter', 'Segoe UI', Roboto, sans-serif" font-size="94" font-weight="800" letter-spacing="-2" fill="#f8fafc">
    Path<tspan fill="#f59e0b">d</tspan>
  </text>
</svg>
