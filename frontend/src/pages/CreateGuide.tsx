import React from 'react';

export const CreateGuide: React.FC = () => {
  return (
    <div className="glass-card" style={{ maxWidth: 800, margin: '2rem auto', textAlign: 'left' }}>
      <h2>Crear Nueva Guía</h2>
      <p style={{ color: 'var(--text-secondary)', margin: '1rem 0' }}>Formulario interactivo para construir orden cronológico de obras (Próximamente)...</p>
    </div>
  );
};
export default CreateGuide;
